<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.edittext" parent="AppTheme">
        <item name="colorAccent">#ffff</item>
        <item name="android:textColorHint">#ffff</item>

        <item name="colorControlNormal">@color/colorAccent</item>

    </style>
    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/year</item>
        <item name="colorButtonNormal">@color/title</item>
    </style>
</resources>
